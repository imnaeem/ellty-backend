// graphQL query field enums (to be populated via mongoose)
export enum FieldNames {
  USERS = 'users',
  CALCULATIONS = 'calculations',
  AUTHOR = 'author',
  COMMUNICATION = 'communication',
  PARENT_CALCULATION = 'parentCalculation',
}
